# Generated by scripts/kjob_spike.py - a spike, not deployed.
#
# The step would run:
#   kubectl kjob create job --profile tpu-tpu7x-standard-1t-1x1x1 \
#     --localqueue tpu7x-standard-1t-1x1x1 \
#     --time 10800s \
#     --cmd '<command>'
#
# Blocked: the workload image differs per build and `kjobctl create
# job` has no flag for it, so the image below stays a placeholder
# and this CR cannot be applied as written.
apiVersion: kjobctl.x-k8s.io/v1alpha1
kind: JobTemplate
metadata:
  name: tpu-tpu7x-standard-1t-1x1x1
  namespace: buildkite
template:
  parallelism: 1
  completions: 1
  backoffLimit: 0
  podFailurePolicy:
    rules:
    - action: Ignore
      onPodConditions:
      - type: DisruptionTarget
    - action: FailJob
      onExitCodes:
        containerName: workload
        operator: NotIn
        values:
        - 0
  template:
    metadata:
      annotations:
        gke-gcsfuse/volumes: 'true'
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'false'
        gke-gcsfuse/metadata-prefetch-memory-limit: 2Gi
        gke-gcsfuse/cpu-limit: '0'
        gke-gcsfuse/memory-limit: '0'
    spec:
      restartPolicy: Never
      serviceAccountName: tpu-workload
      nodeSelector:
        cloud.google.com/gke-tpu-accelerator: tpu7x
        cloud.google.com/gke-tpu-topology: 1x1x1
      tolerations:
      - key: google.com/tpu
        operator: Equal
        value: present
        effect: NoSchedule
      containers:
      - name: workload
        image: ${IMAGE}
        command:
        - /bin/bash
        - -lc
        env:
        - name: HF_HOME
          value: /cache/hf
        - name: PYTHONUNBUFFERED
          value: '1'
        - name: VLLM_XLA_CACHE_PATH
          value: /cache/jax
        - name: JAX_COMPILATION_CACHE_DIR
          value: /cache/jax
        resources:
          limits:
            google.com/tpu: '1'
          requests:
            google.com/tpu: '1'
        volumeMounts:
        - name: dshm
          mountPath: /dev/shm
        - name: jax-cache
          mountPath: /cache/jax
        - name: hf-cache
          mountPath: /cache/hf/hub
      volumes:
      - name: gke-gcsfuse-cache
        emptyDir:
          medium: Memory
          sizeLimit: 111Gi
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 16Gi
      - name: jax-cache
        persistentVolumeClaim:
          claimName: jax-cache
      - name: hf-cache
        persistentVolumeClaim:
          claimName: hf-cache
---
apiVersion: kjobctl.x-k8s.io/v1alpha1
kind: ApplicationProfile
metadata:
  name: tpu-tpu7x-standard-1t-1x1x1
  namespace: buildkite
spec:
  supportedModes:
  - name: Job
    template: tpu-tpu7x-standard-1t-1x1x1
    requiredFlags:
    - cmd
    - localqueue
    - time
